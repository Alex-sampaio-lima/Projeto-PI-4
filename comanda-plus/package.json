{
  "name": "comanda-plus",
  "version": "1.0.0",
  "description": "Monorepo do projeto Comanda+ — App de pedidos full stack",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm install && node server.js",
    "mobile": "cd mobile && npm install && npx expo start",
    "install:all": "cd backend && npm install && cd ../mobile && npm install"
  },
  "author": "Comanda+ Team",
  "license": "MIT"
}

# Variáveis de ambiente do App Mobile — Exemplo

# URL da API backend
# IMPORTANTE: Substitua SEU_IP pelo IP local da máquina (ex: 192.168.1.10)
# NÃO use localhost — o Expo roda no dispositivo físico/emulador
EXPO_PUBLIC_API_URL=http://SEU_IP:3000/api
